# Copy to .env.local (gitignored) for local dev, and set the same key in Vercel's
# project environment variables for production. Get the value from
# Google Analytics -> Admin -> Data streams -> your web stream -> Measurement ID.
VITE_GA_MEASUREMENT_ID=
